seed: 42
mode: "multi_stage"               # Options: "single_stage" or "multi_stage"
constitutive_eq: "maxwell_B"      # Options: "maxwell_B", "oldroyd_B", "ptt_exponential"

use_projection: false
target_cond: null                 # conditioning target (overridden by mode in code)
stage_range: null                 # stage range tuple (overridden by mode in code)

n_samples: 10000
dim: 3
eta0: 1.0
lam: 0.5
lam_r: 1.0                        # Only needed for oldroyd_B
max_vorticity_ratio: 1

paths:
  images: "images"                # Base folder for all images
  data: "datafiles"               # Base folder for all saved datasets

hydra:
  job:
    chdir: False